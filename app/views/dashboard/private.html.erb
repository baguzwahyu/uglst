<h2>My User-Groups</h2>
<%= simple_form_for @user do |f| %>
  <%= f.input :full_name %>
  <%= f.input :short_name %>
  <%= f.button :submit %>
<% end %>
<%= render partial: 'user_groups_table', locals: { user: @user } %>
